namespace _Project.Scripts.Actors {
    /// <summary>
    /// Capture when an object has additional behavior when being destroyed
    /// </summary>
    public interface IDisposable {
        void Dispose();
    }
}