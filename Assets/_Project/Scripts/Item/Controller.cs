using System;
using UnityEngine;

namespace FPS.Item {
    public class Controller : MonoBehaviour {
        private void OnTriggerEnter(Collider other) {
            //if (TryGetComponent())
        }
    }
}

